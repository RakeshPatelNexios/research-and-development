Check IndexedDB Support in JavaScript

You can test if IndexedDB is available in a browser using:

if (window.indexedDB) {
    console.log("IndexedDB is supported!");
} else {
    console.log("IndexedDB is NOT supported in this browser.");
}


For real-time compatibility checks, you can visit Can I Use IndexedDB. 🚀

*https://caniuse.com/IndexedDB*
